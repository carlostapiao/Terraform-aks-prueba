apiVersion: v1
kind: Service
metadata:
  name: convenio-service
  labels:
    app: convenio-api
spec:
  type: LoadBalancer
  selector:
    app: convenio-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4040